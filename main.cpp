#include <iostream>
#include "Test.h"
#include "StudentGUI.h"
#include "GUI.h"

int main(int argc, char* argv[])
{
    //testAll();
    //interface(argc, argv);
    gui_start(argc, argv);
    return 0;
}
